 <h1>Lista</h1>  

<%= form_with model: @todo, url: edit_path, local: true do |f| %>

  <div>
    <%= f.label :descrición %><br>
    <%= f.text_area :description %>
  </div>
  <div>
    <%= f.label :completado %>
    <%= f.check_box :completed %>
  </div>
  <div>
    <%= f.submit 'Enviar' %>
  
  </div>
<% end %>